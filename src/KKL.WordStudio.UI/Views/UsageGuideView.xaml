<UserControl x:Class="KKL.WordStudio.UI.Views.UsageGuideView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Background="{DynamicResource Brush.App.Background}">
 <Grid Margin="14">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Grid.Row="0" CornerRadius="16" Padding="20,16" Background="#FF1D4ED8">
   <DockPanel>
    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
     <Button Content="Düzenleme Modu" Command="{Binding ToggleEditModeCommand}" Style="{StaticResource Btn.Mini}" Margin="0,0,8,0"/>
     <Button Content="Kaydet" Command="{Binding SaveEditsCommand}" Style="{StaticResource Btn.Mini}" Margin="0,0,8,0" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibility}}"/>
     <Button Content="Varsayılana Dön" Command="{Binding ResetEditsCommand}" Style="{StaticResource Btn.Mini}" Margin="0,0,8,0" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibility}}"/>
     <Button x:Name="CloseGuideButton" Content="Çalışma Alanına Dön" Style="{StaticResource Btn.Mini}"/>
    </StackPanel>
    <StackPanel><TextBlock Text="KKL Word Studio Kullanım Kılavuzu" Foreground="White" FontSize="22" FontWeight="Bold"/><TextBlock Text="Meyveli demo veri üzerinden ekran bazlı kullanım anlatımı" Foreground="#FFDCEBFF"/></StackPanel>
   </DockPanel>
  </Border>
  <Grid Grid.Row="2">
   <Grid.ColumnDefinitions><ColumnDefinition Width="285"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Border Grid.Column="0" Background="White" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="10">
    <DockPanel><TextBlock DockPanel.Dock="Top" Text="BÖLÜMLER" FontWeight="Bold" Margin="7,6,0,10"/><ListBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection, Mode=TwoWay}" BorderThickness="0"><ListBox.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal" Margin="6"><TextBlock Text="{Binding Icon}" Width="28"/><TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></DockPanel>
   </Border>
   <Border Grid.Column="2" Background="White" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="14">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
     <StackPanel Margin="22" DataContext="{Binding SelectedSection}">
      <TextBlock Text="{Binding Title}" FontSize="23" FontWeight="Bold" Foreground="#FF102A56"/>
      <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Margin="0,8" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
      <Border Background="#FFF8FAFD" BorderBrush="#FFD8E1ED" BorderThickness="1" CornerRadius="12" Padding="10">
       <Grid><Image Source="{Binding ImageSource}" Stretch="Uniform" MaxHeight="720" RenderOptions.BitmapScalingMode="HighQuality"/><Button Content="Görseli Değiştir" Command="{Binding ChangeImageCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource Btn.Mini}" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/></Grid>
      </Border>
      <GroupBox Header="Ne işe yarar?" Margin="0,12,0,0" Padding="10"><StackPanel><TextBlock Text="{Binding Purpose}" TextWrapping="Wrap"/><TextBox Text="{Binding Purpose, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="70" Margin="0,8,0,0" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/></StackPanel></GroupBox>
      <GroupBox Header="Adım adım kullanım" Margin="0,10,0,0" Padding="10"><StackPanel><ItemsControl ItemsSource="{Binding Actions}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="•  {Binding}" TextWrapping="Wrap" Margin="0,3"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBox Text="{Binding ActionsText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="120" Margin="0,8,0,0" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/></StackPanel></GroupBox>
      <GroupBox Header="İpucu / Dikkat" Margin="0,10,0,22" Padding="10"><StackPanel><TextBlock Text="{Binding Tip}" TextWrapping="Wrap"/><TextBox Text="{Binding Tip, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="70" Margin="0,8,0,0" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/></StackPanel></GroupBox>
     </StackPanel>
    </ScrollViewer>
   </Border>
  </Grid>
 </Grid>
</UserControl>