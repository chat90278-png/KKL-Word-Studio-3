<UserControl x:Class="KKL.WordStudio.UI.Views.ProjectExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <HierarchicalDataTemplate x:Key="ExplorerNodeTemplate" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Name}" />
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Proje Gezgini" FontWeight="Medium" Margin="8,8,8,4" />
        <TreeView x:Name="Tree" ItemsSource="{Binding RootNodes}" ItemTemplate="{StaticResource ExplorerNodeTemplate}"
                  BorderThickness="0" SelectedItemChanged="Tree_SelectedItemChanged" />
    </DockPanel>
</UserControl>
